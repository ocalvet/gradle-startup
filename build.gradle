plugins {
    id "base"
}

task zip(type: Zip, group: "Archive", description: "Archives sources in a zip file") {
    from "src"
}

task copy(type: Copy, group: "Custom", description: "Copy files from src to dest") {
    from "src"
    into "dest"
}
